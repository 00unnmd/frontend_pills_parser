import { Card, CardContent, CardHeader } from "@mui/material";

const Dashboard = () => {
  return (
    <Card>
      <CardHeader title="Добро пожаловать в pills_parser" />
      <CardContent>Инструкция по использованию:</CardContent>
    </Card>
  );
};

export default Dashboard;
